.TH @PROGNAME@ 1 "2004 Oct 14"
.SH NAME
@PROGNAME@ \- displays financial data
.SH SYNOPSIS
@PROGNAME@ <data-file>
.SH USAGE
@PROGNAME@ displays the financial data contained in the supplied <data-file>. The
data file must have been created by GnuCash; @PROGNAME@ will tell you if there are
any problems with the file.

Keys are similar to both vi (j, k) and emacs (C-N, C-P). Hitting Enter on an
account displays that account's register; hitting enter on a register entry
displays all the splits for that transaction.
